{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "nodemon --watch './**/*.ts' --exec 'bun run index.ts'",
    "dev": "nodemon --watch './**/*.ts' --exec 'bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.0",
    "nodemon": "^3.1.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@fal-ai/client": "^1.2.3",
    "common": "*",
    "db": "*",
    "express": "^4.21.2"
  }
}
